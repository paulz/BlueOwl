platform :ios, '11.0'

target 'BlueOwl' do
    use_frameworks!

    target 'AppSpecs' do
        inherit! :search_paths
        pod 'Quick'
        pod 'Nimble'
    end

    target 'UITests' do
        inherit! :search_paths
    end
end
