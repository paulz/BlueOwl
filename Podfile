platform :ios, '12.0'

target 'BlueOwl' do
    use_frameworks!

    target 'UnitSpecs' do
        inherit! :search_paths
        pod 'Quick'
        pod 'Nimble'
    end

    target 'UITests' do
        inherit! :search_paths
    end
end
